/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['demo.awaikenthemes.com', 'media.istockphoto.com', 'mir-s3-cdn-cf.behance.net', 'raw.githubusercontent.com']
    },
    basePath: '',
    trailingSlash: true
}

export default nextConfig